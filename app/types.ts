type Routes = {
  id: string;
  name: string;
  url: string;
};

type UserData = {
  preferences: string;
  "bean-type": string;
  quantity: string;
  "grind-option": string;
  deliveries: string;
};
